---
import { cn } from "$src/util";

interface Props {
	class?: string;
}

const className = Astro.props.class || "";
---

<article class={cn("mb-4 overflow-hidden rounded-lg bg-base-300/75 shadow-xl ring-1 ring-gray-900/5", className)}>
	<slot />
</article>
